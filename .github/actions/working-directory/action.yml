name: should ignore a change under the working directory
description: Checks that a change under the working directory is ignored without the allowlist naming it.
runs:
  using: composite
  steps:
    - uses: ./
      with:
        act: touch build-output.txt
